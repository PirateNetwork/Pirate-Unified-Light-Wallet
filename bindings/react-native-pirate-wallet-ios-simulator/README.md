# react-native-pirate-wallet-ios-simulator

`react-native-pirate-wallet-ios-simulator` contains the universal iOS
simulator XCFramework slice used by `react-native-pirate-wallet`.

Applications should depend on `react-native-pirate-wallet`. The wrapper
installs this package automatically on macOS.
